# Traceability Map (generated)

> このファイルは `iori-spec trace report` が front matter の `trace.*` から生成するビューです。SSOT は各 spec の front matter であり、本ファイルは手書き・直接編集しないでください。

| REQ | IF | DATA | TEST |
| --- | -- | ---- | ---- |
| *(run `iori-spec trace report` to populate)* | | | |
